name: tidyflow
description: TidyFlow Pro - Интеллектуальный оптимизатор системы с аналитикой
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  path_provider: ^2.1.0
  permission_handler: ^11.0.0
  flutter_local_notifications: ^16.0.0
  image: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
